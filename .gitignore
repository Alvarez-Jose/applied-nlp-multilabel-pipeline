service_account.jsonlogs/*!logs/.gitkeep__pycache__/*.pyc.vscode/.claude/review_output/# DeBERTa weights — stored on HuggingFace Hub (jalva182/palestine-violence-deberta)# Download automatically on first inference run; ~738 MB cached by transformersmodeling/saved_models/deberta_v3_multilabel/deberta_v3_best/model.safetensorsmodeling/saved_models/deberta_v3_multilabel/deberta_v3_best/spm.modelmodeling/saved_models/deberta_v3_multilabel/deberta_v3_best/training_args.bin# Training checkpoints — intermediate artifacts, not needed for inferencemodeling/saved_models/deberta_v3_multilabel/runs/# Version checkpoints created by the Streamlit UI (local backups only)modeling/backups/# Lab workflow directories — track folder structure (.gitkeep) but not contentdata/incoming/*!data/incoming/.gitkeepdata/review_exports/*!data/review_exports/.gitkeepdata/reviewed/*!data/reviewed/.gitkeepdata/training/*!data/training/.gitkeep# === Added for public release: research data and derived weights stay private ===# Raw and processed research data (private; not for public)data/raw/*data/processed/*!data/raw/.gitkeep!data/processed/.gitkeep# Training-data copy used by modeling (derived from private data)modeling/training/data/# Trained baseline model weights (derived from private data — could leak training rows via inference)modeling/saved_models/baseline_*/*.joblibmodeling/saved_models/baseline_*/*.pkl# Internal team docs that should not be publicdocs/meeting_notes.md